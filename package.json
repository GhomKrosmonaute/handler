{
  "name": "@ghom/handler",
  "version": "1.2.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "File handler that extends @ghom/event-emitter",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "build": "tsc",
    "test": "npm run build && jest tests/test.js",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "typescript": "^4.5.5",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.5"
  },
  "dependencies": {
    "@ghom/event-emitter": "^1.1.0"
  }
}
